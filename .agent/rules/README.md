# Rules — Governance Layer

Rules are **mandatory laws** the agent must follow. They load at boot in numeric order and define identity, integrity, verification gates, context memory, and safety protocols. Think of them as the constitution.

See `docs/maps/SYSTEM_MAP.md` (⚙️ Core Infrastructure) for which rules belong to which domain.
