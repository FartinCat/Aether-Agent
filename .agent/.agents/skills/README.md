# Agent Personas — Specialist Identities

Each folder is one **agent persona** with its own `SKILL.md` (instructions) and `agents/openai.yaml` (OpenAI-compatible definition). These are WHO the system becomes for specific tasks — a scanner, a code reviewer, a Python specialist, etc.

See `docs/maps/SYSTEM_MAP.md` for which agents belong to which domain.
