# Workflows — Executable Pipelines

Workflows are **multi-step recipes** that orchestrate agents, skills, and rules into complete operations. Each file defines a pipeline (scan → spec → build → test → release). The numeric prefix defines lifecycle order.

See `docs/maps/SYSTEM_MAP.md` for which workflows belong to which domain category.
