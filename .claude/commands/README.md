# Slash Commands — Entry Points

Each file defines a `/command` that Claude Code discovers and loads. These are the user-facing entry points that invoke agent personas and workflows under the hood.

See `docs/maps/SYSTEM_MAP.md` for which commands belong to which domain.
