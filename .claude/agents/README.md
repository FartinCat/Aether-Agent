# Specialist Personas — Claude Code Agents

These are Claude Code-specific agent definitions (code-reviewer, security-auditor, test-engineer). They differ from the main agent personas in `.agent/.agents/skills/` — these are formatted for Claude Code's native agent system.

See `docs/maps/SYSTEM_MAP.md` for their domain categories.
